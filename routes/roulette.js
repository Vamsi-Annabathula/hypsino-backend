const express = require("express");
const router = express.Router();

//const { rechargeBalance } = require("../controllers/payment");

//router.post("/rechargeBalance", rechargeBalance);

module.exports = router;
